{
  "name": "bstar-skymp-patches",
  "version": "1.0.0",
  "private": true,
  "description": "Patch content for BStar Launcher (SkyMP mods + Platform data)",
  "scripts": {
    "build-patch": "node scripts/build-patch.js",
    "generate-manifest": "node scripts/generate-manifest.js"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.700.0"
  }
}
