# Launcher setup

In the **BStarRP_SkyMP_Launcher** repo, set in `.env`:

```env
PATCH_GITHUB_REPO=YourOrg/BStarRP_SkyMP_Data
```

Replace `YourOrg` with your GitHub org or username. The launcher will use this repo’s latest release for **Check for updates**.
